'use strict';

function mapToView(resource) {
    return resource;
}

function mapToViewList(list) {
    return list;
}

module.exports = {
    mapToView: mapToView,
    mapToViewList: mapToViewList
};
