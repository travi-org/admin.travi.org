build:
  - clean
  - bower
  - sass
  - uglify

lint:
  - jscs
  - jslint
  - eslint

unit: mochacov:unit

integration: cucumberjs:integration

test:
  - unit
  - integration

verify:
  - lint
  - test

heroku:production: build

default: verify
