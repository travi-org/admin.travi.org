compile:
  - sass
  - webpack:build
  - uglify

build:
  - clean
  - bower
  - compile

lint:
  - scsslint
  - eslint

unit: mochacov:unit

integration: cucumberjs:integration
wip: cucumberjs:wip

test:
  - unit
  - integration

verify:
  - lint
  - test

heroku:production: build

default:
  - build
  - verify
