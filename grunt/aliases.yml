build:
  - clean
  - bower
  - sass

lint:
  - jscs
  - jslint

unit: mochacov:unit

integration: cucumberjs:integration

test:
  - unit
  - integration

verify:
  - lint
  - test

heroku:production: build

default: verify
