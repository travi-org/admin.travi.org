build:
  - clean
  - sass

lint:
  - jscs
  - jslint

unit: mochacov:unit

integration: cucumberjs

test:
  - unit
  - integration

verify:
  - lint
  - test

heroku:production: build

default: verify
