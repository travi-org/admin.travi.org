compileClient:
  - sass
  - webpack:build
  - uglify

compileServer: babel

build:
  - clean
  - bower
  - compileClient

lint:
  - scsslint
  - eslint

unit: mochacov:unit

integration: cucumberjs:integration
wip: cucumberjs:wip

test:
  - unit
  - integration

verify:
  - lint
  - test

heroku:production: build

default:
  - build
  - verify
