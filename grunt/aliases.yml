build:
  - clean
  - bower
  - sass

lint:
  - jscs
  - jslint
  - eslint

unit: mochacov:unit

integration: cucumberjs:integration

test:
  - unit
  - integration

verify:
  - lint
  - test

heroku:production: build

default: verify
