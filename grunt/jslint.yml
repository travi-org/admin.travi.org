app:
  src: index.js
  directives:
    node: true
    unparam: true
    nomen: true
  options:
    errorsOnly: true

test:
  src: test/**/*.js
  directives:
    node: true
    regexp: true
    nomen: true
    predef:
      - suite
      - setup
      - teardown
      - test
      - assert
      - sinon
  options:
    errorsOnly: true