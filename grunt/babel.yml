options:
  sourceMap: true

dist:
  files:
    - expand: true
      cwd: lib/server
      src: '**/*.js*'
      dest: dist/server

    - expand: true
      cwd: lib/shared
      src: '**/*.js*'
      dest: dist/shared
