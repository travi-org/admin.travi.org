options:
  files: test/unit/**/*-test.js*

unit:
  options:
    reporter: spec

junit:
  options:
    reporter: mocha-junit-reporter

coverage:
  files: test/unit/**/*-test.js
  options:
    coveralls: true

