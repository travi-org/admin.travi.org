machine:
  node:
    version:
      0.12.0

dependencies:
  pre:
    - npm install -g cucumber

test:
  post:
    - grunt mochacov:coverage
    - mkdir -p $CIRCLE_TEST_REPORTS/cucumber
    - cucumber.js --format=json test/integration/features/ > $CIRCLE_TEST_REPORTS/cucumber/tests.cucumber