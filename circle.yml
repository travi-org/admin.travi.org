machine:
  node:
    version:
      0.12.0

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/cucumber
    - cucumber.js --format=json test/acceptance/features | $CIRCLE_TEST_REPORTS/cucumber/tests.cucumber